{
  "name": "ecom-company-agents",
  "version": "0.1.0",
  "private": true,
  "description": "ECC OmniStudio — AI agent harness + e-commerce platform for POD & dropshipping",
  "scripts": {
    "dev": "concurrently \"pnpm --filter @ecc/daemon dev\" \"pnpm --filter @ecc/web dev\"",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "playwright": "^1.61.1"
  },
  "dependencies": {
    "@tanstack/react-router": "^1.170.16",
    "@types/react": "18.3.3",
    "@types/react-dom": "18.3.0",
    "lucide-react": "0.400.0",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1",
    "zod": "^4.4.3"
  }
}
